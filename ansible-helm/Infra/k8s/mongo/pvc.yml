apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-pvc
  labels:
    app: mongo
    env: production
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: mongo-storage
  resources:
    requests:
      storage: 8Gi
